upstream frontend {
    server frontend:4200;
}

upstream backend {
    server backend:5000;
}

upstream webhook {
    server webhook:8000;
}

server {
    listen 80;
    listen [::]:80;

    server_name newecopeople.com www.newecopeople.com;

    location / {
        rewrite ^ https://$host$request_uri? permanent;
    }

    #for certbot challenges (renewal process)
    location ~ /.well-known/acme-challenge {
        allow all;
        root /data/letsencrypt;
    }

}

#https://newecopeople.com
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name newecopeople.com;

    server_tokens off;

    ssl_certificate /etc/letsencrypt/live/newecopeople.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/newecopeople.com/privkey.pem;

    ssl_buffer_size 8k;

    ssl_dhparam /etc/ssl/certs/dhparam-2048.pem;

    ssl_protocols TLSv1.2 TLSv1.1 TLSv1;
    ssl_prefer_server_ciphers on;

    ssl_ciphers ECDH+AESGCM:ECDH+AES256:ECDH+AES128:DH+3DES:!ADH:!AECDH:!MD5;

    ssl_ecdh_curve secp384r1;
    ssl_session_tickets off;

    # OCSP stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 8.8.8.8;

    return 301 https://www.newecopeople.com$request_uri;
}

#https://www.newecopeople.com
server {
    server_name www.newecopeople.com;
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_tokens off;

    # ssl on;

    ssl_buffer_size 8k;
    ssl_dhparam /etc/ssl/certs/dhparam-2048.pem;

    ssl_protocols TLSv1.2 TLSv1.1 TLSv1;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDH+AESGCM:ECDH+AES256:ECDH+AES128:DH+3DES:!ADH:!AECDH:!MD5;

    ssl_ecdh_curve secp384r1;
    ssl_session_tickets off;

        # OCSP stapling
    ssl_stapling on;
    ssl_stapling_verify on;
    resolver 8.8.8.8 8.8.4.4;

    ssl_certificate /etc/letsencrypt/live/newecopeople.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/newecopeople.com/privkey.pem;

    proxy_ssl_verify on;
    proxy_ssl_session_reuse on;
    proxy_ssl_name www.newecopeople.com;

    # security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload";
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    #CSP
    # add_header Content-Security-Policy "frame-src 'self'; default-src 'self'; script-src 'self' 'unsafe-inline' https://maxcdn.bootstrapcdn.com https://ajax.googleapis.com; img-src 'self'; style-src 'self' https://maxcdn.bootstrapcdn.com; font-src 'self' data: https://maxcdn.bootstrapcdn.com; form-action 'self'; default-src 'self' https://fonts.googleapis.com https://fonts.gstatic.com http://www.google-analytics.com 'unsafe-inline'; style-src * 'unsafe-inline;upgrade-insecure-requests;" always;
    # add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://ssl.google-analytics.com https://assets.zendesk.com https://connect.facebook.net; img-src 'self' https://ssl.google-analytics.com https://s-static.ak.facebook.com https://assets.zendesk.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://assets.zendesk.com; font-src 'self' https://themes.googleusercontent.com; frame-src https://assets.zendesk.com https://www.facebook.com https://s-static.ak.facebook.com https://tautt.zendesk.com; object-src 'none'";
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    


    location /api {
        proxy_pass http://backend;
    }

    location /_webhook {
        proxy_pass http://webhook;
    }

    location / {
        rewrite /(.*) /$1 break;
        proxy_pass http://frontend;
    }
}


